GenericType is a component hook factory and a lifecycle inference library.

It's used by SweetTooth in Application.

Provides the type of the hook factory ("sequential" | "concurrent" |
"conditional" | "cyclic").
